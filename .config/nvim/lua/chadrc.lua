---@class ChadrcConfig
local M = {}

M.ui = {
  theme = "nord",
  statusline = {
    theme = "minimal",
    separator_style = "round",
  },
}

-- M.plugins = "plugins"

-- M.mappings = require "mappings"

return M
