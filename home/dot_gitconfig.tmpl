[user]
	name = {{ .name }}
	email = {{ .email }}

[push]
	default = upstream
	autoSetupRemote = true

[pull]
	rebase = true

[init]
	defaultBranch = main

[credential]
    helper = store --file ~/.my-git-credentials